# 7709-财务基础信息

## 作用

批量查询基础财务字段，如流通股本、总股本、EPS、资产、负债、收入、利润等。

| 项目 | 内容 |
| --- | --- |
| 调用方法 | `client.corporate.finance_batch()` / `client.get_finance_batch()` |
| 底层接口 | [`0x0010`](../COMMANDS_7709.md#cmd-0x0010) |
| 返回模型 | `FinanceBatch` |

## 示例

```python
from eltdx import TdxClient

with TdxClient(timeout=3) as client:
    batch = client.corporate.finance_batch(["sz000001", "sh600000"])
    selected = client.corporate.finance_batch(["sz000001"], fields=["流通股本", "total_shares"])
```

读取完整记录：

```python
record = batch.records[0]
print(record.full_code, record.circulating_shares, record.total_assets_yuan)
```

## 参数

| 参数 | 含义 |
| --- | --- |
| `codes` | 单个代码或代码列表 |
| `fields` | 本地字段过滤；不改变服务端请求 |
| `include_raw` | 是否保留原始 payload |
| `refresh` | 仅 `get_finance_batch()` 支持；跳过缓存 |

## 解析字段

| `FinanceRecord` 字段 | 含义 |
| --- | --- |
| `exchange` / `market_id` / `code` / `full_code` | 市场和代码 |
| `finance_info_raw` | 136 字节财务主体原始数据 |
| `liu_tong_gu_ben_raw_float` | 流通股本原始值，单位万股 |
| `province_raw` | 地区 / 地域板块原始编号 |
| `industry_raw` | 通达信粗行业原始编号 |
| `updated_date_raw` | 财务数据更新日期原始值 |
| `updated_date` | 财务数据更新日期 |
| `ipo_date_raw` | 上市日期原始值 |
| `ipo_date` | 上市日期 |
| `zong_gu_ben_raw_float` | 总股本原始值，单位万股 |
| `guo_jia_gu_raw_float` | 国家股原始值，单位万股 |
| `fa_qi_ren_fa_ren_gu_raw_float` | 发起人法人股原始值，单位万股 |
| `fa_ren_gu_raw_float` | 法人股原始值，单位万股 |
| `b_gu_raw_float` | B 股原始值，单位万股 |
| `h_gu_raw_float` | H 股原始值，单位万股 |
| `eps_raw` | 每股收益原始值 |
| `zong_zi_chan_raw_float` | 总资产原始值，单位千元 |
| `liu_dong_zi_chan_raw_float` | 流动资产原始值，单位千元 |
| `gu_ding_zi_chan_raw_float` | 固定资产原始值，单位千元 |
| `wu_xing_zi_chan_raw_float` | 无形资产原始值，单位千元 |
| `gu_dong_ren_shu_raw_float` | 股东人数原始值 |
| `liu_dong_fu_zhai_raw_float` | 流动负债原始值，单位千元 |
| `chang_qi_fu_zhai_raw_float` | 长期负债原始值，单位千元 |
| `zi_ben_gong_ji_jin_raw_float` | 资本公积金原始值，单位千元 |
| `jing_zi_chan_raw_float` | 净资产原始值，单位千元 |
| `zhu_ying_shou_ru_raw_float` | 主营收入 / 营业收入原始值，单位千元 |
| `zhu_ying_li_run_raw_float` | 主营利润原始值，单位千元 |
| `ying_shou_zhang_kuan_raw_float` | 应收账款原始值，单位千元 |
| `ying_ye_li_run_raw_float` | 营业利润原始值，单位千元 |
| `tou_zi_shou_yu_raw_float` | 投资收益原始值，单位千元 |
| `jing_ying_xian_jin_liu_raw_float` | 经营现金流原始值，单位千元 |
| `zong_xian_jin_liu_raw_float` | 总现金流原始值，单位千元 |
| `cun_huo_raw_float` | 存货原始值，单位千元 |
| `li_run_zong_he_raw_float` | 利润总额原始值，单位千元 |
| `shui_hou_li_run_raw_float` | 税后利润原始值，单位千元 |
| `jing_li_run_raw_float` | 净利润 / 归母净利润原始值，单位千元 |
| `wei_fen_li_run_raw_float` | 未分利润原始值，单位千元 |
| `mei_gu_jing_zi_chan_raw_float` | 每股净资产，单位元 |
| `bao_liu_2_raw_float` | 保留字段 2 |
| `record_hex` | 单条原始十六进制 |

| `FinanceBatch` 字段 | 含义 |
| --- | --- |
| `records` | 财务记录列表 |
| `count` | 记录数 |
| `raw_payload` | 原始 payload |

## 派生字段

| 字段 | 计算方式 |
| --- | --- |
| `circulating_shares` | `liu_tong_gu_ben_raw_float * 10000` |
| `total_shares` | `zong_gu_ben_raw_float * 10000` |
| `total_assets_yuan` | `zong_zi_chan_raw_float * 1000` |
| `net_profit_yuan` | `jing_li_run_raw_float * 1000` |

## 字段过滤

`fields` 是 SDK 本地过滤，底层仍按 `0x0010` 请求完整记录。常用中文别名：

| 中文字段 | 实际属性 |
| --- | --- |
| `流通股本` | `circulating_shares` |
| `总股本` | `total_shares` |
| `总资产` | `total_assets_yuan` |
| `净利润` | `net_profit_yuan` |

示例返回：

```python
selected = client.corporate.finance_batch(["sz000001"], fields=["流通股本", "净利润"])
# [{"full_code": "sz000001", "流通股本": ..., "净利润": ...}]
```

## 单位口径

| 字段类型 | 换算 |
| --- | --- |
| 股本类 | raw 单位万股，实际股数 = raw * `10000` |
| 金额类 | raw 单位千元，人民币元 = raw * `1000` |
| 每股类 | raw 直接是元 |
| 日期类 | `YYYYMMDD` 转日期 |

地区和行业中文名当前没有直接暴露到 `FinanceRecord`，模型里保留的是 `province_raw` 和 `industry_raw`。
