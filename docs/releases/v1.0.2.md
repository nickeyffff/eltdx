# v1.0.2

## 修复

- `client.get_adjusted_kline()` / `client.get_adjusted_kline_all()` 现在会透传 `anchor_date`，旧兼容入口也可以直接使用定点前复权 / 定点后复权。
- 修正 `v1.0.1` 发布说明中关于 `get_quote()` 五档盘口兼容行为的表述，避免使用不严谨的“用户期望”措辞。

## 验证

- 已实盘验证 `qfq`、`hfq`、`fixed_qfq` K 线请求可返回正常数据。
- 已通过全量单元测试。
